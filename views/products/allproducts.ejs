<% layout('layouts/boilerplate') %>
    <h1>All Products</h1>
    <ul>
    <% for(product of products) {%>
        <li><a href="/product/<%= product._id %>"> <%= product.title %> </a></li>
        
        <% } %>
    </ul>
